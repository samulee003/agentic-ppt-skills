# SPEAKER-SCRIPT

| Page | Approved wording | Time | Transition | Existing vs remaining | Likely audience challenge | Q&A risk | Trim if short on time |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | "Hello everyone. This Open-slide demo looks at agentic coding as a workflow, not as a vendor product. The important shift is from generating code once to acting, observing, and revising." | 30s | "Let's start with the core loop..." | The deck already had a five-slide structure; vendor attribution was removed. | Is this describing one product? | No; it is a neutral teaching model. | No |
| 2 | "The core loop is Read, Plan, Write, Verify. The agent reads the request and repository, chooses a focused change, applies it, and checks the result. Verification is part of the loop, not an optional final step." | 60s | "To act on the repository, the model needs tools..." | Slide 2 descriptions were raised to 30px and shortened. | How does the loop stop? | Use completion criteria, budgets, and escalation when checks keep failing. | No |
| 3 | "These tool names are illustrative. Real agents expose different interfaces, permissions, and sandboxes. The general role is the same: connect the model to files, commands, search, and test output." | 60s | "A historical benchmark shows how difficult repository repair was..." | The tool panel remains a schematic, not an API reference. | Does run_command mean unrestricted shell access? | No; permissions and sandboxing are implementation choices. | No |
| 4 | "In the original 2023 SWE-bench paper, Claude 2 resolved 1.96 percent of the full 2,294-task benchmark with BM25 retrieval. That is a historical baseline, not a current leaderboard result. An iterative observe-edit-test-retry loop can use execution feedback to shape the next attempt, but this slide does not claim a universal numerical uplift." | 90s | "More action and feedback also require clearer boundaries..." | Replaces the unsupported generic SWE-bench Lite comparison. | Why this number and setup? | It is Table 5 of arXiv:2310.06770 v1; it is not SWE-bench Lite or a no-tools control. | Yes |
| 5 | "Safety should follow the risk of the action. Consequential, irreversible, or out-of-scope actions should trigger human review. Routine, reversible work can stay autonomous when permissions and rollback are clear." | 60s | "Thank you, and let's open for Q&A." | Replaces a universal approval claim with risk-based gating. | Does approval slow the agent down? | Use checkpoints where failure cost is high, not on every low-risk step. | No |
